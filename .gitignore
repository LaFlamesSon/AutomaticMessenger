# Secrets & personal config — never commit these
credentials.json
token.json
config.yaml
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# The canonical Obsidian vault lives in OneDrive and is linked here as a junction.
/context-vault/
