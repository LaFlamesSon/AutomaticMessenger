---
description: Resume CaughtUp from the vault latest-work pointer. Report queues and hold. Do not start product work.
---

# /startup — resume from latest-work

You (this Cursor chat) are the Executive Assistant. Do not spawn the whole
team. Do not start product, acceptance, deploy, or secret work until Yafet
approves.

## Read (only these)

1. `CLAUDE.md`
2. `context-vault/ops/ea-briefings/resume-next-session.md`
3. The newest file in `context-vault/ops/sessions/`
4. Ready or blocked `context-vault/ops/handoffs/latest-*.md`
   (`latest-ea`, `latest-database-agent`, `latest-backend-dev`,
   `latest-frontend-dev`, `latest-qa-agent`, `latest-research-agent`)

Do not dump the vault, `index.md` catalog, or every wiki page.

## Report, then hold

1. **Last work** — commit, deploys, tests, Auto-send fact (do not change it)
2. **Ready** — local actions that need no authorization
3. **Blocked** — items that cannot proceed
4. **Waiting-on-Yafet** — auth, console, mailbox, or recording gates
5. **Next safe local action** vs **auth-required**

Then stop. Wait for Yafet.
