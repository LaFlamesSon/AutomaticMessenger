# CaughtUp www redirect

Minimal Cloudflare Worker that permanently redirects `www.getcaughtup.io` to the canonical apex domain while preserving the path and query string.

It has no bindings, secrets, storage, or user-data processing.
